<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET je potreba jen kvuli log serveru na portu 8765 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="AH EGL Render"
        android:hasCode="false"
        android:allowBackup="false"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        <!-- Zadna Java. System spusti primo nativni knihovnu libeglrender.so.
             KRITICKE: hodnota android.app.lib_name se musi shodovat se jmenem
             knihovny v CMakeLists.txt (eglrender -> libeglrender.so). -->
        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density">
            <meta-data android:name="android.app.lib_name" android:value="eglrender" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
